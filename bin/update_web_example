#!/usr/bin/env bash
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_common.sh"

# Build the flutter_readium web bundle (lib/helpers/readiumReader.js).
(cd "$REPO_ROOT/flutter_readium" && npm run build:dev)

echo "Finished at $(date +'%H:%M')"
