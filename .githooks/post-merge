#!/bin/sh
# codegraph: keep the code index fresh after this git operation.
# Guarded so it is a no-op for anyone without codegraph installed.
command -v codegraph >/dev/null 2>&1 && codegraph sync -q >/dev/null 2>&1 || true
