# Generated fixtures live here after `bin/fetch_test_resources` / `bin/install`.
# Keep generated fixture binaries untracked, but not the placeholder files that
# preserve the asset directory in a fresh clone.
pubs/*
!pubs/.gitignore
