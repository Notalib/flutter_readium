// Jest stub for CSS side-effect imports (e.g. `import "./style.css"`), which
// ts-jest cannot transform. Mapped in jest.config.js via moduleNameMapper.
// Not a test file (does not match testMatch's *.test.ts).
module.exports = {};
